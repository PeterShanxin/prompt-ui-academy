# Keep cloud accounts hidden until the Appwrite project is fully configured.
NEXT_PUBLIC_CLOUD_PROGRESS_ENABLED=false
NEXT_PUBLIC_APPWRITE_ENDPOINT=
NEXT_PUBLIC_APPWRITE_PROJECT_ID=

# Server-only. Requires databases.read, databases.write, users.read, and users.write.
APPWRITE_API_KEY=
